#ifndef CONSTANT_H
#define CONSTANT_H

const int TIME_DELAY_BETWEEN_FRAMES = 100000; // Time delay between frames in microseconds

const int 
    up = 'w',
    down = 's',
    left = 'a',
    right = 'd';

const int 
    before_start = 0,
    after_start = 1;

#endif